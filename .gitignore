# Transient run logs — evidence lives in the *_report.json files, which ARE committed
*.log
__pycache__/
*.pyc

# Built site: generated by pipeline/build_site.py, deployed from CI. Derived, never committed.
_site/
